import React from "react";

const Header = () => {
  // we'll eventually replace the h1 with a logo that the user will select

  return (
    <div>
      <h1 style={{ marginTop: "9%" }}>powerding dashboard</h1>
    </div>
  );
};

export default Header;
